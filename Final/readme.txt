Environment：
	System： 	Windows (so it is a different file path format as Linux. "dbName.setText(dbFileName.substring(dbFileName.lastIndexOf("\\")+1));")
	JDK：		JDK 13.0.2(inetAddress.getHostName() will return 127.0.0.1 instead of "localhost"
				"textQueryArea.append("Client " + clientNo + "'s host name is " + inetAddress.getHostName() + "\n");")
	IDE：		Intellij IDEA

